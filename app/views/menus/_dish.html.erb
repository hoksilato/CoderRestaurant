<div class="card">
  <div class="card-block">
    <h4 class="card-title"><%= link_to dish.name, dish %></h4>
    <h6 class="card-subtitle text-muted"><%= number_to_currency(dish.price, unit: 'đ', format: "%n %u") %></h6>
  </div>
  <%= link_to image_tag(dish.image_url, alt: dish.name), dish %>
  <div class="card-block">
    <p class="card-text"><%= dish.description %></p>
    <a href="#" class="btn btn-primary">Order</a>
  </div>
</div>